#include"pergunta.c"

Pergunta* initPergunta(long, int, int, int, int, int, int, long, char*, char*, int, int, int);

int comparePerguntas(Pergunta*, Pergunta*);

Pergunta* copyPergunta(Pergunta*);

void freePergunta(Pergunta* p);
